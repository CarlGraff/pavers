<!DOCTYPE html>
<html>
<style type="text/css">
   body { background-image: linear-gradient(90deg, #FFFFFF, #1a53ff); !important; } /* Adding !important forces the browser to overwrite the default style applied by Bootstrap */

</style>
<head>
  <title>Paver</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= yield :js %>
  <%= csrf_meta_tags %>
</head>
<body class="foo_me">
<div class="container">
	<% flash.each do |message_type, message| %>
		<% if !message.include?("Paver was") %>
	        <div class="alert alert-<%= message_type %>"><%= message %></div>
	    <% end %>
	<% end %>
	<%= yield %>
	<% if request.original_url().include?("localhost")  %>
	<%= debug(params) if Rails.env.development? %>
	<%= debug session[:paver_params] if Rails.env.development? %>
<!-- 	<%= debug session if Rails.env.development? %>-->
	<% end %>
</div>
</body>
</html>
